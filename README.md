# Inverted-Search
Developed a file-based search engine that builds an inverted index to map words to their occurrences across multiple files. Implemented using linked lists, supporting fast search, update, display, and file persistence.
